#include<bits/stdc++.h> 

using namespace std;

void buble(int n, int a[]) {
	for(int i = 0; i < n; ++i) {
		for(int j = 1; j < n - i; ++j) {
			if(a[j] < a[j - 1]) swap(a[j], a[j - 1]);
		}
	}
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	int n;
	cin >> n;
	int a[n];
	for(int i = 0; i < n; ++i) {
		cin >> a[i];
	}
	buble(n, a);
	for(int i = 0; i < n; ++i) {
		cout << a[i] << " ";
	}
}
